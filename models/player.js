class Player {
    constructor(
      id,
      gameIds,
      charcterName,
      playerName,
      role,
    ) {
      this.id = id
      this.gameIds = gameIds
      this.charcterName = charcterName
      this.playerName = playerName
      this.role = role
    }
  }
  
  export default Player