class Player {
    constructor(
      id,
      gameIds,
      characterName,
      playerName,
      role,
    ) {
      this.id = id
      this.gameIds = gameIds
      this.characterName = characterName
      this.playerName = playerName
      this.role = role
    }
  }
  
  export default Player