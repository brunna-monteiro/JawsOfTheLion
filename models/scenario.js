class Scenario {
  constructor(
    id,
    gameIds,
    title,
    imageUrl,
    goal,
  ) {
    this.id = id;
    this.gameIds = gameIds;
    this.title = title;
    this.imageUrl = imageUrl;
    this.goal = goal;
  }
}

export default Scenario;
